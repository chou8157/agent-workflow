# Agent 工作流设计能力

这个上下文描述一套用于接手、治理和持续改进项目 Agent 工作流的能力。它关注工作流文档、行为规范、项目理解和持续迭代之间的边界，而不是某个具体业务项目的实现细节。

## Language

**工作流生成与迭代 Skill**:
一种封装通用流程、项目接手方法和持续沉淀规则的 Codex skill。它不是固定模板复制器，而是约束 Agent 按可控流程生成、使用并迭代项目专属工作流。
_Avoid_: 万能模板、一次性项目说明、随机生成规范

**通用层**:
跨项目复用的个人工作方式、行为底线、文档层级、标准流程和质量门禁。它为所有项目提供默认约束，但不替代项目内事实。
_Avoid_: 全局项目文档、公共碎片规则

**项目层**:
针对单个项目沉淀的架构、运行逻辑、执行规则、风险、门禁和约定。它由初始化接手时的渐进式阅读生成，并在后续使用中被修正。
_Avoid_: 一次性全量理解、项目百科

**迭代层**:
在持续开发、排障、验收和协作中不断更新工作流的机制。它记录新发现的事实、规则变化、用户偏好、风险和验证结果，使项目工作流越用越贴合。
_Avoid_: 临时聊天记忆、无结构日志

**项目工作流目录**:
一个项目内承载 Agent 工作流程、治理规范、项目理解、验证记录和持续迭代记录的分层目录。它是工作流生成与迭代 Skill 的核心产物，默认目录名为 `.agent-workflow/`。
_Avoid_: 平铺文档目录、agent_docs 固定命名、散落文档

**AGENTS.md 入口**:
项目根目录面向 Agent 的强约束入口文件。它负责先成为一个完整的标准 `AGENTS.md`，再集成项目工作流目录的阅读路径和最高优先级约束；如果已存在则补充这三类内容，如果不存在则先按常见 Agent 标准创建，再补充。
_Avoid_: 工作流主体文档、超长项目百科、重复治理目录内容

**AGENTS 标准化创建**:
创建新的 `AGENTS.md` 时，先按通用 Agent 入口标准生成一个完整版本，再补充项目专属的强约束、工作流导航和关键限制，避免只写工作流附属内容导致入口过薄。
_Avoid_: 只写三类补丁、过薄入口、跳过标准 AGENTS 结构

**工作流骨架**:
`.agent-workflow/` 的默认一级目录固定为 `00-core/`、`10-project/`、`15-modules/`、`20-gates/`、`30-records/`，用于分别承载通用规则、项目理解、模块索引、门禁和持续记录。
_Avoid_: 一级目录平铺、目录数量失控、职责混放

**核心文件固定**:
工作流目录内必须稳定存在的文件包括 `README.md`、`workflow.md`、`current-status.md`、`progress-log.md`、`validation-log.md`、`decision-log.md` 和 `risk-log.md`；其余文件和子目录允许按项目需要扩展。
_Avoid_: 全部文件自由命名、关键入口缺失、结构不可预测

**默认模板**:
`architecture.md`、`conventions.md`、`phase-gates.md` 和模块索引文件应采用固定骨架模板，再按项目事实填充具体内容；模板提供框架，不预设所有细节。
_Avoid_: 无框架自由发挥、模板过细、内容与项目事实脱节

**主动触发更新**:
工作流目录的更新默认由用户明确触发，例如初始化项目、完成一次开发后要求记录、或要求改进工作流规则。Agent 不应在普通任务中无提示地大规模重写工作流文档。
_Avoid_: 自动乱改、隐式重构工作流、每次任务都大规模更新

**写入策略**:
初始化和记录模式默认可以直接写入目标项目的 `AGENTS.md` 与 `.agent-workflow/`；但如果需要覆盖或大幅改写已有 `AGENTS.md`，必须先给出变更计划或 diff 并等待用户确认。
_Avoid_: 静默覆盖入口文件、无确认大改、只生成不落盘

**扫描边界**:
初始化默认只读取当前目标项目内的文件。只有用户明确提供路径、链接，或当前项目文档显式引用外部资料时，才读取项目外资料并标注来源。
_Avoid_: 跨项目规则污染、隐式读取外部仓库、来源不明的规范

**记录更新范围**:
执行记录模式时必须更新 `30-records/progress-log.md` 和 `30-records/current-status.md`；其他文件按事实触发，只有存在验证、风险、稳定决策、待修复项或深度模块阅读时才更新对应记录。
_Avoid_: 每次全量更新、制造文档噪音、无事实记录

**决策记录分层**:
`30-records/decision-log.md` 用于日常轻量决策；ADR 只用于重大、难逆、未来读者可能疑惑且经过真实权衡的决策。默认不强制创建 ADR。
_Avoid_: 所有决策都写 ADR、重大决策只留聊天记录

**ADR 位置**:
ADR 默认存放在 `.agent-workflow/30-records/adr/`；如果目标项目已有 `docs/adr/` 等既有 ADR 位置，则沿用项目现有位置。
_Avoid_: 同一项目多个 ADR 体系、忽略既有文档传统

**改进权限边界**:
改进当前项目工作流时可以直接写入项目内 `.agent-workflow/`；改进总 Skill 自身模板或全局规则时，必须明确说明这是全局规则变更并等待用户确认。
_Avoid_: 项目特例污染全局模板、静默修改 skill 自身规则

**共享工作流目录**:
跨项目或多仓库链路应使用独立的共享工作流目录管理契约、交接、变更清单和下游消费状态。单个项目的 `.agent-workflow/` 只管理本项目事实，不承担跨项目全局治理。
_Avoid_: 把多仓库链路塞进单项目、项目边界混乱

**共享初始化分支**:
总 Skill 应支持初始化跨项目共享工作流目录，但它必须作为独立的 `init shared` 分支存在，不应混入普通单项目初始化流程。
_Avoid_: 单项目初始化默认生成跨项目结构、共享治理和项目治理混杂

**共享目录命名**:
跨项目共享工作流目录默认命名为 `.agent-workflow-shared/`；如果用户明确指定名称，例如 `stack-workflow/`，则使用用户指定名称。
_Avoid_: 强制迁移既有共享目录名、共享目录命名不一致

**单项目优先**:
总 Skill 的主要能力应优先服务单项目 `.agent-workflow/`，多项目共享工作流是低频扩展能力。共享工作流必须规范项目间交互关系和消费逻辑，但不能压过单项目初始化、使用、记录和改进流程。
_Avoid_: 为低频多项目场景牺牲单项目体验、共享逻辑污染单项目流程

**共享交互索引**:
共享工作流目录必须包含项目清单和交互关系索引，例如 `projects.md` 与 `interaction-map.md`，用于说明项目角色、上下游关系、接口契约、数据流和发布顺序。
_Avoid_: 只记录变更不记录关系、上下游职责不清

**共享弱引用**:
参与共享工作流的单项目只在自身 `.agent-workflow/README.md` 或 `10-project/dependencies.md` 中弱引用共享目录位置和参与关系，不复制共享规则。
_Avoid_: 单项目和共享目录重复维护同一规则、双向强耦合

**自然语言路由**:
用户不需要记住固定命令；总 Skill 应将自然语言意图映射到内部模式，例如接手/初始化映射到 `init`，开始开发映射到 `use`，记录/归档映射到 `record`，改规则/以后都这样映射到 `improve`，多项目/共享链路映射到 `init shared`。
_Avoid_: 强迫用户记命令、完全无模式自由发挥

**工作流自检**:
初始化完成后必须执行一次自检，检查 `AGENTS.md` 入口、工作流目录骨架、核心文件、未知项记录和已有规则保护。后续也应提供手动自检入口，但不需要在每次普通迭代中自动触发。
_Avoid_: 初始化后不验证、每次任务都强制自检

**自检修复边界**:
自检发现缺失目录或空模板文件时可以自动补齐；发现 `AGENTS.md` 冲突、规则冲突、需要重写已有内容或迁移大量结构时，只能报告并等待用户确认。不确定事实应进入未知项记录，不能自动补成事实。
_Avoid_: 自动解决高风险冲突、把猜测写成事实

**文档语言**:
默认使用英文文件名和中文正文。英文文件名保障跨项目一致性和工具友好性，中文正文符合主要阅读和协作习惯。
_Avoid_: 文件名中英文混乱、默认英文正文

**规则分级**:
模板中的规则应区分 `必须`、`默认`、`建议` 和 `禁止`。安全、权限、Git、外部副作用和验证底线使用强制语气；普通流程使用默认或建议语气。
_Avoid_: 所有规则同等强制、软建议伪装成硬约束

**事实标注**:
初始化和项目理解文档中的关键事实应轻量标注来源和状态，例如 `已确认`、`待确认` 或 `推断`。推断内容不能被写成无条件规则。
_Avoid_: 来源不明的事实、推断伪装成确认结论

**使用模式加载规则**:
日常开发前必须读取 `AGENTS.md`、`.agent-workflow/README.md` 和 `30-records/current-status.md`；再按任务类型渐进读取核心流程、门禁、模块索引、风险、架构或规范文档。
_Avoid_: 每次全量加载、跳过入口和当前状态

**旧工作流目录处理**:
初始化时若发现既有 `agent_docs/` 或其他工作流目录，不自动删除或迁移；应先识别内容并给出迁移建议，用户确认后再搬迁或整合。若仅参考旧内容生成新目录，必须标注来源。
_Avoid_: 静默迁移、删除旧目录、来源丢失

**验证记录真实性**:
`validation-log.md` 只能记录真实执行过的测试、命令或人工验收；未验证时应记录未执行原因和建议验证动作，不能把主观判断写成验收通过。
_Avoid_: 看起来没问题即通过、伪造验证记录

**偏好文件**:
稳定的用户偏好和协作习惯应单独记录在 `.agent-workflow/00-core/preferences.md`，避免与项目事实、代码规范或门禁混在一起。
_Avoid_: 把个人偏好写成项目事实、偏好散落在多个文档

**偏好提取**:
初始化时可以从现有 `AGENTS.md`、用户明确表达或已确认工作流文档中提取稳定偏好，并标注来源；不能从一次偶然对话推断长期偏好。
_Avoid_: 臆测用户偏好、无来源偏好

**当前状态摘要**:
`30-records/current-status.md` 只保留当前目标、下一步、阻塞、风险摘要和最近更新时间；历史流水应进入 `progress-log.md`，避免当前状态文件膨胀。
_Avoid_: 当前状态历史化、入口上下文过长

**风险与待修复分离**:
`risk-log.md` 记录可能影响开发、发布、安全或维护的风险；`pending-fixes.md` 记录已确认的问题、Bug 或债务修复项，包含现象和建议处理方式。
_Avoid_: 风险变成 Bug 列表、已确认问题埋在风险里

**任务分级**:
通用模板采用轻量三档任务分级：低风险、中风险、高风险。低风险可直接执行，中风险需先只读分析和方案，高风险必须确认方案、验证方式和回滚或止损策略。
_Avoid_: 无风险区分、过度复杂分级

**记录建议**:
当任务包含代码或文档变更、深度模块阅读、发现风险或执行验证时，结束时可以建议用户触发记录模式；普通问答或微小操作不需要提醒。未获用户明确要求时不自动执行记录。
_Avoid_: 每次都提醒记录、自动记录噪音

**迁移模式**:
总 Skill 应提供独立的 `migrate` 内部模式，用于从旧 `agent_docs/`、`stack-workflow/` 或其他既有工作流目录迁移到新结构。迁移必须先给映射方案并等待用户确认。
_Avoid_: 初始化时自动迁移、无映射方案搬迁

**总 Skill**:
一个统一承载初始化、日常使用、记录和优化的主 skill。它通过内部模式和分层文档完成工作流治理，而不是拆成多个入口分散使用。
_Avoid_: 多个平行 skill、入口碎片化

**初始化顺序**:
接手已有项目时的固定执行顺序：先识别并保留根目录 `AGENTS.md`，再创建或补充 `.agent-workflow/` 的最小骨架，随后生成项目层初版与模块索引初版。
_Avoid_: 先写大文档、入口和治理目录混写、跳过入口检查

**初始化最低扫描协议**:
接手新项目时用于生成项目层初版的最小阅读范围。它只要求理解项目大致结构、运行方式、关键入口和高风险区域，并明确记录未知区域，不要求一次性完整阅读全项目。
_Avoid_: 全量代码审计、伪完整项目理解

**模块阅读记录**:
在具体开发或排障中深度阅读某个模块后留下的轻量索引。它只记录代码位置、模块职责、关键入口和已知注意事项，详细逻辑仍应在下次修改前重新阅读确认。
_Avoid_: 复制代码说明、过度详细模块百科、永久替代源码阅读

**模块索引目录**:
`.agent-workflow/15-modules/` 下按模块拆分存放模块阅读记录的目录。它承载渐进式项目理解，避免把模块细节塞进全局架构文档。
_Avoid_: architecture.md 堆积、模块说明平铺、替代源码
