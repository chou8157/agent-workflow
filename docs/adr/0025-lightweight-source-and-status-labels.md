# Label project understanding with lightweight source and status

We decided that key facts captured during initialization and project understanding should include lightweight source and status labels, such as confirmed, pending confirmation, or inferred. This is especially important in architecture, dependencies, and unknowns documents, because inferred project understanding must not be promoted into unconditional rules.
