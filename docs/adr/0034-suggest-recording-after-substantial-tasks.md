# Suggest recording after substantial tasks, but do not record automatically

We decided that the workflow may suggest record mode after tasks involving code or documentation changes, deep module reading, discovered risks, or executed validation. Routine Q&A and tiny actions should not trigger reminders. Recording still requires an explicit user request, which keeps documentation updates intentional and avoids noisy churn.
