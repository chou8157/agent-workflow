# Use default templates for core workflow docs

We decided that the core project docs in `.agent-workflow/` should use fixed templates: `architecture.md`, `conventions.md`, `phase-gates.md`, and the module index files all start from a shared structure and are then filled with project facts. This balances consistency and flexibility: the skill can generate predictable documentation without pretending to know every detail up front.
