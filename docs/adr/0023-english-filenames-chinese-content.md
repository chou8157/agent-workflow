# Use English filenames and Chinese document content by default

We decided that workflow templates should use stable English filenames and Chinese body content by default. English filenames keep paths consistent and tool-friendly across projects, while Chinese content matches the user's normal reading and collaboration style.
