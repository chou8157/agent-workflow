# Optimize for single-project workflows first

We decided that the master skill should prioritize single-project `.agent-workflow/` initialization, usage, recording, and improvement. Multi-project shared workflows are supported as a secondary, lower-frequency capability with their own interaction and consumption rules. Shared workflow directories default to `.agent-workflow-shared/`, unless the user explicitly chooses another name such as `stack-workflow/`.
