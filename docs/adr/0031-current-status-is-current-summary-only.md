# Keep `current-status.md` as a current summary only

We decided that `30-records/current-status.md` should contain only the current goal, next step, blockers, risk summary, and last updated time. Historical activity belongs in `progress-log.md`. This keeps the project re-entry point short and prevents normal work history from bloating the current context file.
