# Require initialization self-checks and support manual checks later

We decided that initialization must end with a workflow self-check covering the `AGENTS.md` entry, `.agent-workflow/` skeleton, core files, unknown-item handling, and preservation of existing rules. Later iterations should expose a manual self-check entry point, but ordinary tasks should not automatically run full checks every time.
