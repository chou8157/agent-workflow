# Shared workflows must include project and interaction indexes

We decided that a shared workflow directory must include explicit indexes for participating projects and their interactions, such as `projects.md` and `interaction-map.md`. Cross-project governance is not just a pile of change manifests: future agents need to understand project roles, upstream and downstream relationships, contracts, data flow, and release order before consuming a change.
