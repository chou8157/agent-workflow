# 共享 智能体工作流

本目录用于管理多项目或多仓库链路中的契约、交接、变更清单和下游消费状态。

## 必读

1. `projects.md`
2. `interaction-map.md`
3. 当前功能对应的 `handoffs/` 和 `change-manifests/`

## 目录说明

- `contracts/`：稳定契约。
- `handoffs/`：当前功能交接。
- `change-manifests/`：单次变更影响面。
- `impact-tracking/`：下游消费状态。
- `records/`：共享工作流记录。
