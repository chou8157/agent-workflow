# 行为原则

## 必须

- 必须尊重项目现有规则和用户已有改动。
- 必须把事实、推断和未知项分开。
- 必须在高风险操作前确认。
- 必须记录真实验证结果，不得伪造。

## 默认

- 默认小步修改，避免无关重构。
- 默认优先使用项目现有工具和风格。
- 默认只在需要时渐进读取更多文档。

## 禁止

- 禁止为了看起来完整而编造项目事实。
- 禁止静默覆盖入口文件或旧工作流目录。
