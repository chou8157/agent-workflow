# 进展记录

## 记录规则

- 记录真实发生的开发、阅读、排障或文档变更。
- 历史写在这里，当前摘要写入 `current-status.md`。

## 记录

### 2026-07-03 - 需求澄清与领域建模

- 目标：设计一套通用的项目智能体工作流生成与迭代能力。
- 变更：通过持续追问确认核心产品形态、目录结构、初始化策略、记录策略、自检策略、多项目边界和迁移策略。
- 结果：形成 `CONTEXT.md` 和 `docs/adr/0001...0035`。
- 后续：基于确认后的设计搭建总 skill。

### 2026-07-04 - 搭建 `agent-workflow` skill 初版

- 目标：实现一个总 skill，支持初始化、使用、记录、改进、自检、迁移和共享工作流。
- 变更：创建 `agent-workflow/SKILL.md`、`references/`、`assets/templates/`、`scripts/` 和 `agents/openai.yaml`。
- 结果：完成初版结构和模板，脚本可生成单项目和共享工作流目录。
- 后续：进行评估和小优化。

### 2026-07-04 - TDD 开发自检与脚手架脚本

- 目标：保证目录生成和结构检查行为稳定。
- 变更：使用测试驱动实现 `check_workflow.py` 和 `scaffold_workflow.py`。
- 结果：覆盖空项目缺失检查、完整项目通过检查、低风险补齐、空项目脚手架、已有 `AGENTS.md` 不重写等行为。
- 后续：进行 skill 校验和前向测试。

### 2026-07-04 - 达尔文式评估与小优化

- 目标：评估 `agent-workflow` 的触发、结构、边界和实测表现。
- 变更：创建 `test-prompts.json` 和 `evaluation-notes.md`，补充中文触发词、record 检查清单、半残缺工作流处理规则。
- 结果：干跑评分约 84.2/100；基础验证和本地前向测试通过。
- 后续：真实项目试用后继续迭代。

### 2026-07-06 - 安装到 Codex skills

- 目标：让后续 Codex session 可以通过 `$agent-workflow` 调用该 skill。
- 变更：将 `agent-workflow/` 复制到 `/Users/yizhoucp/.codex/skills/agent-workflow`。
- 结果：安装版校验通过，模板资产完整。
- 后续：重启 Codex 后试用。

### 2026-07-06 - 中文化修复

- 目标：修复当前项目文件内容英文过多，不符合“中文为主”的问题。
- 变更：中文化 `SKILL.md`、`evaluation-notes.md`、脚本帮助文本、脚本输出文本、reference 模式标题和 UI 默认 prompt。
- 结果：本地版和安装版均已同步，skill 校验、测试和脚本编译通过。
- 后续：把当前项目记录为可接手状态。

### 2026-07-06 - 初始化当前项目工作流

- 目标：降低新 session 接手成本。
- 变更：为当前项目创建 `AGENTS.md` 和 `.agent-workflow/`，并补充当前状态、进展、验证、风险和项目概览。
- 结果：新 session 可从 `AGENTS.md`、`.agent-workflow/README.md` 和 `.agent-workflow/30-records/current-status.md` 接手。
- 后续：重启 Codex 后继续真实项目试用。

### 2026-07-15 - 多工作项上下文能力第一期

- 目标：让项目默认支持多个需求、任务和缺陷的独立上下文恢复，同时保留单一项目工作流。
- 变更：增加工作项模板、任务管理脚本、任务级 use/record 路由、兼容自检和项目内测试；当前项目也启用工作项入口。
- 结果：本地测试、严格结构自检和 skill 格式校验通过。
- 后续：经确认后同步安装版，并在低风险真实项目试跑。

### 2026-07-15 - 工作流版本化升级能力

- 目标：让已生成工作流能安全跟进最新结构，不覆盖用户已有内容。
- 变更：引入结构版本文件、升级计划与安全升级脚本；新增入口文档建议文件机制和旧版本兼容测试。
- 结果：当前项目升级为版本 2，项目内测试、结构自检、升级状态和 skill 格式校验通过。
- 后续：经确认后同步安装版，在真实项目试跑 v1 到 v2 升级。

### 2026-07-15 - 升级能力审查修复

- 目标：确保已标记最新版但结构缺失的工作流仍能恢复到完整结构。
- 变更：升级脚本始终检查当前版本必需入口；最新版本改为读取模板版本文件；新增回归测试。
- 结果：项目内测试 `7 passed`，当前项目升级状态仍为最新。
- 后续：同步安装版前继续保留真实项目试跑门禁。

### 2026-07-16 - 同步安装版

- 目标：让重启后的 Codex 可加载多工作项和版本化升级能力。
- 变更：将本地 `agent-workflow/` 保留式同步到 Codex skills 安装目录。
- 结果：安装版 skill 格式校验通过，排除缓存目录后与本地版无差异。
- 后续：重启 Codex 后选低风险真实项目试跑。
