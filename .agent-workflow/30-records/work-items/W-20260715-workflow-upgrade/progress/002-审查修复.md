# 002-审查修复

## 发现

审查发现升级脚本只在版本低于 2 时补齐工作项入口。若项目已标记为版本 2 但该入口被误删，脚本会误报为最新，无法恢复结构。

## 修复

- 无论当前版本是否最新，升级计划都会检查并补齐当前版本要求的工作项入口。
- `LATEST_VERSION` 改为直接读取模板中的 `WORKFLOW_VERSION`，消除脚本常量与模板文件的双重维护。
- 新增回归测试，覆盖“版本已最新但结构缺失”的升级恢复场景。

## 验证

- `python3 -m pytest tests -q`：7 passed。
- 当前项目升级状态为版本 2、无待审阅项。
