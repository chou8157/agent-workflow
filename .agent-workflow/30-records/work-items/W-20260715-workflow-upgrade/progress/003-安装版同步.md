# 003-安装版同步

## 目标

将已验证的本地 `agent-workflow` 同步到 Codex skills 安装目录。

## 实际变更

- 使用保留式同步将本地 `agent-workflow/` 更新到 `/Users/yizhoucp/.codex/skills/agent-workflow/`。
- 排除 `__pycache__/`，不删除安装版现有文件。

## 验证

- 对安装版执行 `quick_validate.py`，结果通过。
- 排除缓存目录后对比本地版与安装版，未发现差异。

## 后续

- 重启 Codex 后，在低风险真实项目试跑多工作项与版本化升级能力。
