# 工作项入口

恢复此工作项时，按以下顺序读取：

1. 项目根 `AGENTS.md` 和 `.agent-workflow/README.md`。
2. 本目录的 `current-status.md`。
3. 本目录的 `work-item.md`。
4. 如涉及代码状态，读取 `code-state.md`；如需追溯细节，再按需读取 `changes/`、`progress/` 和局部日志。

本目录只记录当前工作项的事实，不复制项目公共规则、架构或其他工作项的进展。
