# 变更记录

## 1.0.0 - 2026-09-14

首个完整产品版本，包含工作流结构版本 5。

- 工作项使用 `.state.json` 作为生命周期唯一机器事实源。
- 内部记录与外部交付物分平面管理，交付物支持 staging、published、archive 和来源追溯。
- 初始化默认为 `minimum` 能力，可按依赖启用 `decisions`、`knowledge`、`evidence`、`delivery`、`audit` 等模块。
- 实质开发、测试和交付结束后自动收口目标工作项，使用指纹保证幂等，不自动猜测完成状态。
- 检查、升级、能力启用和脚手架入口统一能力路径定义。
- 通过 29 项自动化测试、脚本编译、工作流检查和自托管试用验证。

### 兼容与升级

- `WORKFLOW_VERSION` 是结构版本，不等同于产品版本；升级脚本仍负责结构迁移。
- 已有项目不会因能力启用而覆盖已有文件；建议先运行 `upgrade status`，再按工作项验证。
- 安装到 `/Users/yizhoucp/.codex/skills/agent-workflow` 属于独立的工作区外操作，本版本不隐式执行。
